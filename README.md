# Описнаие итогового проекта

Данный TODO_list является итоговым проектом для ЯндексПрактикум.
Он умеет отслеживать задачи, корректировать их и удалять.

При обычном запуске будет отображаться на localhost:7540.

Задания со "звездочкой":
При нахождении файла TODO_PORT в окружении в основной директории отобразится на указанном порту.
При нахождении файла TODO_DBFILE в окружении в основной директории откроит альтернативную базу данных.
В функции NextDate реализованны методы для назначения дней недели и дней в месяце.
Добавлена авторизация по паролю из файла "TODO_PASSWORD" в основной директории.

Тесты выполняются без ошибок при конфигурации settings.go:
var Port = 7540
var DBFile = "../scheduler.db"
var FullNextDate = true
var Search = true
var Token = ``